apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "agent-platform.fullname" . }}-workers
  labels:
    app: workers
spec:
  replicas: 1
  selector:
    matchLabels:
      app: workers
  template:
    metadata:
      labels:
        app: workers
    spec:
      {{- with .Values.imagePullSecrets }}
      imagePullSecrets:
        {{- toYaml . | nindent 8 }}
      {{- if .Values.workers.runtimeClassName }}
      runtimeClassName: {{ .Values.workers.runtimeClassName }}
      {{- end }}
      securityContext:
        runAsNonRoot: true
        runAsUser: 1000
        runAsGroup: 1000
        fsGroup: 1000
      containers:
        - name: temporal-worker
          image: "{{ .Values.global.image.repository | default "ghcr.io/gordon-control-plane" }}/agent-platform-worker:{{ .Values.global.image.tag | default "latest" }}"
          securityContext:
            readOnlyRootFilesystem: true
            allowPrivilegeEscalation: false
            capabilities:
              drop:
                - ALL
          resources:
            requests:
              cpu: "100m"
              memory: "128Mi"
            limits:
              cpu: "500m"
              memory: "512Mi"
          volumeMounts:
            - name: tmp-volume
              mountPath: /tmp
          env:
            - name: UNIFIED_API_URL
              value: 'http://{{ include "agent-platform.fullname" . }}-caddy:8080'
            - name: TEMPORAL_URL
              value: '{{ .Release.Name }}-temporal-frontend:7233'
            - name: OPENAI_BASE_URL
              value: 'http://{{ include "agent-platform.fullname" . }}-caddy:8080/v1'
            - name: LITELLM_URL
              value: 'http://{{ include "agent-platform.fullname" . }}-caddy:8080/v1'
          command: ["python", "-m", "orchestrator.temporal_worker"]
      volumes:
        - name: tmp-volume
          emptyDir: {}
---
apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: {{ include "agent-platform.fullname" . }}-workers-netpol
spec:
  podSelector:
    matchLabels:
      app: workers
  policyTypes:
    - Egress
  egress:
    - to:
        - namespaceSelector: {}
      ports:
        - protocol: UDP
          port: 53
        - protocol: TCP
          port: 53
    - to:
        - podSelector:
            matchLabels:
              app.kubernetes.io/name: temporal
      ports:
        - protocol: TCP
          port: 7233
    - to:
        - podSelector:
            matchLabels:
              app: tailscale-ingress
      ports:
        - protocol: TCP
          port: 8080
