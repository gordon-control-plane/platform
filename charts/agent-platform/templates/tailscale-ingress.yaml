{{- if .Values.tailscaleIngress.enabled }}
apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "agent-platform.fullname" . }}-caddy-config
data:
  Caddyfile: |
{{ tpl .Values.tailscaleIngress.caddyConfig . | indent 4 }}
---
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "agent-platform.fullname" . }}-tailscale-ingress
---
apiVersion: rbac.authorization.k8s.io/v1
kind: Role
metadata:
  name: {{ include "agent-platform.fullname" . }}-tailscale-ingress
rules:
- apiGroups: [""]
  resources: ["secrets"]
  verbs: ["create"]
- apiGroups: [""]
  resources: ["secrets"]
  resourceNames: ["{{ include "agent-platform.fullname" . }}-tailscale-state"]
  verbs: ["get", "update", "patch"]
---
apiVersion: rbac.authorization.k8s.io/v1
kind: RoleBinding
metadata:
  name: {{ include "agent-platform.fullname" . }}-tailscale-ingress
subjects:
- kind: ServiceAccount
  name: {{ include "agent-platform.fullname" . }}-tailscale-ingress
  namespace: {{ .Release.Namespace }}
roleRef:
  kind: Role
  name: {{ include "agent-platform.fullname" . }}-tailscale-ingress
  apiGroup: rbac.authorization.k8s.io
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "agent-platform.fullname" . }}-tailscale-ingress
  labels:
    app: tailscale-ingress
spec:
  replicas: 1
  strategy:
    type: Recreate
  selector:
    matchLabels:
      app: tailscale-ingress
  template:
    metadata:
      labels:
        app: tailscale-ingress
      annotations:
        kube-linter.io/ignore-checks: "env-var-secret"
        checksum/config: {{ .Values.tailscaleIngress.caddyConfig | sha256sum | quote }}
    spec:
      securityContext:
        fsGroup: 1000
      serviceAccountName: {{ include "agent-platform.fullname" . }}-tailscale-ingress
      containers:
        - name: caddy
          image: "{{ .Values.tailscaleIngress.image.repository }}:{{ .Values.tailscaleIngress.image.tag }}"
          imagePullPolicy: IfNotPresent
          env:
            - name: TS_USERSPACE
              value: "true"
            - name: TS_KUBE_SECRET
              value: {{ include "agent-platform.fullname" . }}-tailscale-state
            - name: TS_AUTHKEY
              valueFrom:
                secretKeyRef:
                  name: {{ .Values.tailscaleIngress.secretName }}
                  key: TS_AUTHKEY
            - name: TS_HOSTNAME
              value: {{ include "agent-platform.tailscaleHostname" . | quote }}
            - name: TS_STATE_DIR
              value: /data/tailscale
            - name: XDG_CONFIG_HOME
              value: /data/caddy/config
            - name: XDG_DATA_HOME
              value: /data/caddy/data
          volumeMounts:
            - name: caddy-config
              mountPath: /etc/caddy/Caddyfile
              subPath: Caddyfile
            - name: tailscale-run
              mountPath: /tmp
            - name: data-volume
              mountPath: /data
          ports:
            - containerPort: 443
              protocol: TCP
          resources:
            requests:
              cpu: 10m
            limits:
              cpu: 100m
              memory: 128Mi
          securityContext:
            runAsUser: 1000
            runAsGroup: 1000
            runAsNonRoot: true
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities:
              drop:
                - ALL
      volumes:
        - name: data-volume
          persistentVolumeClaim:
            claimName: {{ include "agent-platform.fullname" . }}-tailscale-data
        - name: caddy-config
          configMap:
            name: {{ include "agent-platform.fullname" . }}-caddy-config
        - name: tailscale-run
          emptyDir: {}
{{- end }}
